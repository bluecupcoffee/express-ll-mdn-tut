extends layout

block content
  h1= title
  if book_list.length
    h5 Total Books:  #{book_list.length}
    ul
        each book in book_list
            li
                a(href=book.get('url')) #{book.get('title')}
                |  (#{book.get('Author')['name']})

  else
    p There are no books.